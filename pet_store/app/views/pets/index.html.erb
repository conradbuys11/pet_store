<h1>List of all <%= @header_name %></h1>
<ul>
<% @pets.each do |pet| %>
    <li> <%= link_to(pet[:name], pet_path(pet)) %> </li>
    <% end %>
</ul>

<div><%= link_to("Back to homepage", "/") %></div>